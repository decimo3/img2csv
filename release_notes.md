# Relative Information Acquisition

Due to issues with form images of different resolutions and positions, the algorithm for information acquisition has been updated.

The script now no longer uses SLICES, which are fixed positions, but instead uses relative positions to those referring to the headers, making detection more precise.
